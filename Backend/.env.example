PORT=3001

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=cafe_estoque

# Chave secreta usada para assinar/validar os tokens JWT. Use um valor
# longo e aleatório em produção (nunca reaproveite o valor de exemplo).
JWT_SECRET=troque_por_uma_chave_secreta_forte
